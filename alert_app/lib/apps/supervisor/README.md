# Supervisor App

This folder contains supervisor pages and components.

How to add a supervisor page:
1. Create `lib/apps/supervisor/pages/<page>.dart`.
2. Use shared services in `lib/services/`.
3. Add the route to `lib/main_supervisor.dart`.

Entrypoint: `lib/main_supervisor.dart` — runs the Supervisor app.