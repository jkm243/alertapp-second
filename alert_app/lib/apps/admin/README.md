# Admin App

This folder contains admin pages and components.

How to add an admin page:
1. Create `lib/apps/admin/pages/<page>.dart`.
2. Use shared services in `lib/services/`.
3. Add the route to `lib/main_admin.dart`.

Entrypoint: `lib/main_admin.dart` — runs the Admin app.